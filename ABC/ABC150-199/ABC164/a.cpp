#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int s,w;
    cin >> s >> w;
    cout << (w >= s ? "unsafe" : "safe") << endl;
    
    return 0;
}